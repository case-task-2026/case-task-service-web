{
  "name": "case-task-service-web",
  "version": "0.0.1",
  "description": "Frontend web application for managing caseworker tasks",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/server.js",
    "test": "jest --runInBand",
    "check": "npm run build && npm test"
  },
  "keywords": [],
  "author": "Candidate",
  "license": "UNLICENSED",
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^5.0.0",
    "govuk-frontend": "^6.0.0",
    "nunjucks": "^3.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/jest": "^29.0.0",
    "@types/node": "^22.0.0",
    "@types/nunjucks": "^3.0.0",
    "@types/supertest": "^6.0.0",
    "jest": "^29.0.0",
    "supertest": "^7.0.0",
    "ts-jest": "^29.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  }
}